/* =========================================================
   tests/helpers/globalTeardown.js
   يُشغَّل مرة واحدة بعد انتهاء كل ملفات الاختبار (Jest globalTeardown)
   لا يحتاج تنظيفاً كبيراً لأن كل ملف اختبار يُغلق اتصاله الخاص بنفسه
   عبر setupAfterEnv، لكنه موجود كخطوة أمان أخيرة ولإغلاق أي اتصال متبقٍ
   ========================================================= */

module.exports = async function globalTeardown() {
  // لا حاجة لإجراء حالياً، موجود للتوسع المستقبلي
  // (مثل حذف قاعدة بيانات مؤقتة بالكامل إذا تغيّرت الاستراتيجية لاحقاً)
};
